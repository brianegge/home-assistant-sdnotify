{
  "config": {
    "step": {
      "user": {
        "title": "systemd Notify",
        "description": "This integration sends systemd watchdog and ready notifications. Click submit to set up."
      }
    },
    "abort": {
      "already_configured": "systemd Notify is already configured."
    }
  }
}
